doctype
html(lang=config.language)
    head
        include head
    body
        #mask(style="display: none;")
            img#mask-image(src="#" style=" ")
        .wrap
            header
                a.title-link(href=url_for())
                    p #{config.title.toUpperCase()}

                include nav
            section.container
                block container
            footer
                block pagination
                block copyright
        include scripts